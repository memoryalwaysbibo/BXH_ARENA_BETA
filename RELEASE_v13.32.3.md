# v13.32.3｜手機 QR 儲存與活動分享辨識改善

基底：ac00bb493ff77f226e60b6de4b903aaef2d32833（v13.32.2），僅 BXH_ARENA_BETA。

## 修正

- 分享預設提供「複製活動資訊」「分享到 LINE」，帶活動名稱、獎品、開獎時間（台灣時間）及原活動深層網址。「只複製網址」保留於折疊區。
- QR 生成有活動名稱、白邊的 PNG 圖片，在對話框直接顯示，供長按儲存；新增系統圖片分享，檔案在點擊前準備，避免失去 iOS 使用者啟動狀態。
- 不支援檔案分享或權限失敗時保留長按儲存方式及 Safari 開啟指引；取消分享不顯示成功。下載 PNG 檔不再暗示已存入相簿。
- 分享仍限已公布、非 TEST 活動；不改參加、登入、獎項或開獎權限。

## 驗證

- node --check raffle-ui.js：通過。
- index.html 兩段內嵌 JavaScript 語法檢查：通過。
- node tests/raffle-ui.cjs：通過既有抽獎 UI、權限隔離、登入返回及回放測試。
- node tests/raffle-share.cjs：通過活動文字、時區、不可分享狀態、API 不支援、同步叫用分享、取消、失敗備援測試。
- 本機 Playwright 缺少 Chromium 執行檔，未完成瀏覽器畫面驗收。iPhone Safari／LINE 內建瀏覽器實機儲存與分享待使用者驗收；不宣稱已存入相簿。

## 範圍與限制

無 Firebase Functions、Firestore 規則或資料遷移。主站版本與 raffle-ui.js 快取參數均升為 v13.32.3。
LINE 分享文字已附活動資訊；裸網址的自動預覽卡片仍使用網站共用 OG 標題。既有抽獎後端只有 Callable 與排程，沒有 HTML 分享端點；動態 OG 預覽需要新增並部署活動分享頁，本版未新增該服務。

## 手機驗收

重新載入 Beta，確認 v13.32.3 → 會員抽獎 → 已公布活動 → 分享活動／QR。
1. 複製活動資訊後貼入 LINE，確認活動名稱、獎品與網址；或點「分享到 LINE」。
2. 長按 QR 圖片選擇儲存影像，或點「分享 QR 圖片」選擇儲存／傳送。
3. 確認相簿有圖片，掃碼返回同一活動；未登入時仍沿用登入後返回。
4. 取消分享不應顯示分享完成；QR 載入失敗仍可複製資訊。
