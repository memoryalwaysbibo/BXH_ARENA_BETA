# v13.33.0｜BXH CALL 裁判叫號與單次 PASS

裁判台通知雙方；選手於「我的賽程」開啟智慧叫號後可回覆正在過去、PASS／申請稍候，以及核准後的我已準備好。裁判核准即可，不要求對手按同意；對手可看到暫緩狀態。

PASS 僅限未計分且目前叫號場次，核准時固定同台、同 bracket 與 round 的後兩場；A→B→C→D 改成 B→C→A→D。不足兩場不可核准，不跨輪；一般跳過仍有原功能，PASS 場次及其前置場次禁止再次跳過／承接移台。兩場完成且回到原台後再叫號，最多產生一次該次回補通知。

每位會員每場賽事成功 PASS 一次，每場對戰僅一次；拒絕不扣次數。後端 Firestore 交易一起扣額度、調整排程、更新公開鏡像、保存審核紀錄；固定 operationId 去重。舊叫號回覆拒絕；不同台裁判、非本人回覆、停用帳號拒絕。身分以既有 registrationUid／playerUid 綁定，不以同名猜測；手動名單未綁會員者不能使用雙方通知。

新增 courtCallService（Beta only）與 courtCalls/{code}（只經後端存取）。callRevision 由後端更新，Rules 拒絕舊 serialized runtime／mirror 覆蓋。一般前端同步改為交易一併寫私人與公開資料，核准 PASS 後使用最新狀態。

前端每 5 秒輪詢可見賽事，需停留於裁判台／我的賽程；無背景／鎖屏推播，無語音廣播，未寄送站內信，未自動判棄權。叫號與 PASS 操作均記錄於 audit，不主動對真實玩家測試。

本機核心與前端佇列、身份隔離、名片／inline 語法、賽事回歸測試通過。部署流程須再通過 Firestore 真交易併發測試及完整 Rules 舊快照拒絕測試，才部署 courtCallService、firestore:rules 至 bxh-arena-beta；流程合併原始 ZIP 與既有各功能補丁。實際 Run 及結果見交接檔。

尚未完成手機實機、真人裁判／選手雙裝置登入端到端驗收。拉霸視覺升級與背景推播不在此版，仍待施工。
