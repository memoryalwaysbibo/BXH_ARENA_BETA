# v13.31.0｜我的道具基礎系統

日期：2026/09/16（台灣時間）。前端基底：26ae4320a97a4cbe772066cbc15afb7d77c8b8fc。

## 功能範圍
- 玩家中心獨立「我的道具」分頁：圖片、名稱、數量、用途、期限、來源、取得時間與發放／使用紀錄。
- 同一道具代碼按批次分開顯示，不合併不同期限與來源。每頁 25 筆，可載入更多。
- 最高管理員於道具頁搜尋玩家，選定對象並確認發放；其他管理員及 Tester 不可發放。
- 圖片以選填 HTTPS 網址設定。道具代碼限小寫英數字、底線、連字號，供後續活動資格串接。
- 道具、紀錄、站內信與未讀數同一後端交易成功；失敗不留部分成果。
- 發放前在 sessionStorage 保存原請求，逾時或刷新後按「確認原發放結果」重試，同一操作不重複發放／通知。瀏覽器無法保存操作時，不送出發放。
- 未確認請求暫鎖表單，確認成功後才可建立下一筆；不應在另一視窗另發新單來代替重試。
- 自動重繪保留表單；切換帳號忽略舊回應。站內信每次開啟會重新讀取。
- 道具不可轉讓；過期仍保留數量及紀錄，不可供後續活動使用。

## 刻意留待後續版本
- 抽獎報名、持票條件、消耗票券及活動取消退票：與抽獎模組一同實作。現在沒有單獨扣票按鈕或 API。
- 自動抽獎、得獎通知、領獎及回放仍未施工。
- 本版沒有圖片檔案上傳，圖片使用網址。

## 部署追溯
- 後端功能提交：1a39be588cdcb2c08d064fcc37e58d1e31884d35；模擬器測試入口修正：0e512ff95b3717e5f68194d2aee1b5bcfd4ae2a0。
- 工作流程：後端 `.github/workflows/deploy-beta-inventory.yml`，合併 ZIP 與 mood/profile/pioneer/mailbox/presence/inventory 最新補丁。
- Firebase：bxh-arena-beta；只部署 inventoryService，其他 Functions 不重新部署。
- 前端：本文件所屬 Git 提交及其 Pages Run。部署執行狀態另記於交接文件；此發布說明本身不代表部署成功。
- 前端需同時保留 index.html 與 inventory-ui.js；後者是本版新增的道具介面程式。
- 首次 Run 35015125527 因模擬器測試程式使用不相容的 Admin SDK 入口而失敗，未執行部署；已改用與專案相同的模組化 API，再跑後續 Run。
- 後續 Run 35015314721：Firestore emulator 真實交易檢查及 Beta inventoryService 部署步驟成功；完整工作流程結論另記交接文件。

## 已執行的本機驗證
- inventory-ui.cjs：逾時／刷新重試、連點、帳號隔離、草稿保留、台灣日期與到期、文字轉義與前端發放權限。
- 後端 service-test.cjs：並行原子發放、回滾、權限撤銷、重試內容衝突、到期、分頁、跨帳號隔離，並使用既有 mailbox 程式驗證通知與已讀／未讀統計。
- 既有 mailbox-ui、player-card（含內嵌 script 語法）、settings-draft、title-recipient、tournament-regression 通過。
- 既有 referee-view 與 storage-adapter 測試通過。
- CI 部署前另執行 Firestore emulator 真實交易與查詢驗證，Run 結果另留底。

## 尚未實測／限制
- 尚未進行真實登入帳號發放→收件→道具查閱端到端驗收，也未進行手機實機測試。
- 沒有為測試向真實玩家發道具或寄信。
- tester-ownership Rules 測試本次未執行；本版沒有變更 Rules，新增資料由後端存取。
- sessionStorage 重試保護限該瀏覽器工作階段，清除網站資料或關閉工作階段後須以操作紀錄查核；不同 operationId 是不同發放。

## 保留既有規則
開拓者自動發放規則、稱號按儲存才同步名片、心情小棧每日 10 則／24 小時／每人兩則輪播均不修改。
