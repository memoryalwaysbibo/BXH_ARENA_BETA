# BXH ARENA Beta v13.39.0

## 每日簽到｜補簽機制

- 每位玩家每月可免費補簽 **2 次**。
- 補簽次數每月 1 日重置，不可累積到次月。
- 僅能補簽 **本月** 的漏簽日期。
- 僅能補簽距今天 **7 天內** 的漏簽日期。
- 不可補今天、未來日期或已簽到日期。
- 補簽成功後視同該日期正常簽到，會重新計算：
  - 連續簽到天數
  - 本月簽到天數
  - 累積簽到天數
  - 最高連續簽到
- 補簽紀錄使用 `source: makeup` 保存，便於未來獎勵、活動或統計區分。
- 補簽仍可觸發既有簽到稱號進度。

## 玩家介面

- 每日簽到頁新增「補簽」區塊。
- 顯示本月已使用／剩餘補簽次數。
- 顯示最近 7 天可補簽日期。
- 日曆中可補簽日期會顯示「補」標記，可直接點選。
- 補簽前會二次確認日期與消耗次數。

## 後端

- 新增 callable function：`makeupCheckIn`。
- `getEngagementSnapshot` 新增補簽額度與可補日期資訊。
- 所有補簽限制由 Cloud Function 驗證，前端不可自行繞過。
- 本次僅部署至 `bxh-arena-beta`。

## 尚未導入

- 每日簽到實體／虛擬小獎品。
- 使用活躍積分兌換額外補簽次數。
