# v13.32.4｜賽事看板全螢幕與手機排版修正

基底：b17aeee96bd72cf47399e5ef807c0caeb04b5733（v13.32.3 手機 QR 儲存與活動分享辨識改善）。本版只修改前端看板呈現，不改賽事比賽、名單、比分、勝敗、排名、抽獎或後端規則。

## 查核結果
使用者提供 10.37 秒手機錄影及截圖。錄影可見賽事看板內嵌另一層標準對戰表，重複標題與列印／分享，下載圖片保留工具列及局部內容。截圖另有使用者回報全螢幕按鈕沒有反應。

程式確認：全螢幕僅處理同步拋錯，缺少 API 時無回饋、Promise 失敗未處理；匯出直接擷取固定高度且 overflow:hidden 的看板；看板標題寫死「BXH 戰鬥陀螺賽事」、房號使用本機 ID 尾碼拼出。沒有查到本區混入抽獎資料的證據，亦未查核實際 Firestore 賽事資料。

## 修正
- 「全螢幕對戰表」表示整場對戰表，並非單場計分卡。原生 API 缺少／失敗使用頁內滿版，提供退出按鈕及 Escape，離開頁面恢復捲動。頁內滿版可能保留瀏覽器網址列。
- 標題顯示實際賽事名稱、實際雲端房號；無房號顯示本機賽事，不再拼接假房號。
- 手機工具列分行、較大點擊範圍、配色獨立一列；隱藏看板內重複標題、列印、分享與冠軍區。不可重新抽籤時不顯示該工具，既有後端／操作權限不變。
- 單淘看板固定使用完整樹狀圖，標準對戰表仍保留原本精簡模式；縮放後重畫連線修正座標比例。
- 匯出在 html2canvas 副本展開完整內容，移除工具列與裁切；不改畫面目前縮放、平移；依尺寸限制輸出像素，避免大型籤表固定兩倍輸出耗盡記憶體。

## 測試
node tests/board-view.cjs 通過：原生 API 成功、缺少、拒絕，Escape／退出／離頁還原，真實名稱及房號，強制完整樹狀圖，副本展開及原畫面縮放不變、賽事資料不變。

node tests/player-card.cjs、tests/tournament-regression.cjs、tests/raffle-share.cjs 通過；含 inline 腳本語法與 v13.32.3 分享回歸。git diff --check 通過。

尚未完成 iPhone／Safari 實機、真人登入端到端、真實瀏覽器圖片輸出與手機視覺驗收。本機 DOM 替身測試不等同這些驗收。部署成功證據見同一提交的 Pages Run 與交接文件。
